# Leave empty for local development (Vite proxies /api to localhost:8787).
# For GitHub Pages, set this to the public API origin plus /api, for example:
# VITE_LEADERBOARD_API_BASE=https://your-api.example.com/api
VITE_LEADERBOARD_API_BASE=
