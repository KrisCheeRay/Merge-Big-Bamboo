{
  "name": "merge-bamboo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:liquid": "vite --config vite.liquid.config.js --configLoader native --host 0.0.0.0",
    "build": "vite build",
    "build:liquid": "vite build --config vite.liquid.config.js --configLoader native",
    "build:site": "node fanpage/build-site.mjs",
    "preview": "vite preview --configLoader native --host 0.0.0.0"
  },
  "dependencies": {
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
